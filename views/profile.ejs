<!DOCTYPE html>
<html lang="en">
    <head>
        <title>Profile</title>
        <meta charset="UTF-8">
        <meta name="viewport" content="width=device-width, initial-scale=1">
        <link href="css/style.css" rel="stylesheet">
    </head>
    <body>
        <h1>Profile Page</h1>
        <a href="/logout">Logout</a>

        <h3>Facebook</h3>
            <% if (user.facebook.token) { %>
                <p>
                    <strong>id</strong>: <%= user.facebook.id %><br>
                    <strong>token</strong>: <%= user.facebook.token %><br>
                    <strong>email</strong>: <%= user.facebook.email %><br>
                    <strong>firstname</strong>: <%= user.facebook.name.first %><br>
                    <strong>lasname</strong>: <%= user.facebook.name.last %><br>
                </p>
                <a href="/unlink/facebook">Unlink</a>
            <% } else { %>
                <a href="/connect/facebook">Connect Facebook</a>
            <% } %>
        <h3>User</h3>
            <% if (user.local) { %>
                <p>
                    <strong>id</strong>: <%= user._id %><br>
                    <strong>email</strong>: <%= user.local.email %><br>
                    <strong>firstname</strong>: <%= user.local.name.first %><br>
                    <strong>lastname</strong>: <%= user.local.name.last %><br>
                </p>
            <% } %>
    </body>
</html>